language: php

php:
  - 5.5
  - 5.6
  - 7.0

cache:
  directories:
    - vendor
    - ~/.composer/cache
    - ~/cphalcon

before_install:
  - bin/install-phalcon.sh
  - composer install --prefer-source --no-interaction

script:
  - vendor/bin/phpunit

notifications:
  email: false